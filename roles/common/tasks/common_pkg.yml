---
- name: ensure common packages are installed
  apt: name={{item}} state=present update_cache=yes
  sudo: true
  with_items:
    - vim
    - zsh
    - git
    - tmux
    - screen
    - ack-grep
    - tree
    - ctags
